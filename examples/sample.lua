-- Scratch buffer for trying nxvim-snippets (see examples/init.lua for the triggers).
-- In insert mode, type one of: lf | req | log | today
-- then accept the completion row with <C-y> and use <C-j>/<C-l> and <C-k>/<C-h> to
-- jump tabstops.

local M = {}

return M
